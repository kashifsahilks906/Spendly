# Expense Tracker

A simple Flask-based expense tracker, built step by step as a learning project.

## Setup

```bash
python -m venv venv
venv\Scripts\activate   # Windows
pip install -r requirements.txt
python app.py
```

## Stack

- Flask
- pytest / pytest-flask for testing
